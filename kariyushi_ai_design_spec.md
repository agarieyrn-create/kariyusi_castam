# オリジナルかりゆしウェア AI提案システム 設計書（MVP版）

**バージョン**: v0.2.0  
**更新日**: 2026-06-06  
**著者**: Kariyushi Design System Team  

---

## 1. 概要

### 1-1. システム名
オリジナルかりゆしウェア AI提案システム

### 1-2. システム目的
ユーザーが簡単な質問に回答するだけで、AIが沖縄らしさを反映したオリジナルかりゆしウェアを複数提案する。
単なる画像生成ではなく、あらかじめ登録された「柄」「モチーフ」「配色」「シャツ型」「ロゴ配置」などの素材DBを活用し、AIが最適な組み合わせを提案する。

### 1-3. 想定ユーザー
- ホテル、飲食店、カフェ
- イベント事業者、観光事業者、自治体
- 一般ユーザー、店頭来店ユーザー

### 1-4. MVPゴール
- 簡単な質問回答からAIによる3案提案
- かりゆしウェアの2D/3Dプレビューと簡易編集
- 着用モデル・サイズレポート確認
- 概算見積りの算出と問い合わせ仕様書保存

---

## 2. システムコンセプト

### 2-1. 基本思想
本システムは「自由にデザインするツール」ではなく、「AIが沖縄らしいデザインを提案し、ユーザーはその中から選び、微調整する」体験を重視する。

### 2-2. UX方針
入力負荷を最小化し、「AIに任せられる」体験を優先する。

### 2-3. AIの役割
AIはゼロから画像を生成するのではなく、素材DBを検索・スコアリングし、最適な組み合わせを提案、コンセプト文を生成する「デザインディレクター」として動作する。

---

## 3. ユーザーフロー

### 3-1. 基本フロー
```text
TOP (ヒーロー)
↓
Step 1: 条件入力 (使用シーン/印象/基本色/モチーフ/数量)
↓
Step 2: AI提案比較 (3案をレーン表示)
↓
Step 3: 柄・衿型・ロゴ位置編集
↓
Step 4: 2D Design Studio (ロゴ画像アップロード/テキスト配置)
↓
Step 5: 3Dサイズシミュレーション
↓
Step 6: 仕様書確認 & 問い合わせ保存
```

---

## 🔍 現行MVP実装との差分

本設計書ではNext.js / React等のWebアプリケーション構成を推奨しているが、**現行MVPプロトタイプ**は、開発速度とポータビリティを最優先し、以下の構成で実装されている。

- **画面構成**: 単一の `index.html` 内でのタブ・ウィザード切り替えによるシングルページ構成 (SPA)
- **技術スタック**: Vanilla JS (純粋なJavaScript) + CSS変数 + Three.js + Fabric.js (CDN経由)
- **データベース**: クライアントサイドでのモックDB (`scripts/mock-db.js`) および `localStorage` を使用した擬似永続化
- **状態管理**: グローバルな `state` オブジェクトと状態変更時に再描画するシンプルな `renderAll()` イベント駆動設計

---

## 4. 画面一覧 (MVP実装対比)

| 画面 (設計書) | MVPプロトタイプ内対応セクション | 内容 |
|---|---|---|
| TOP | `#top` (Hero セクション) | サービス紹介とメインプレビュー |
| デザイン開始 | `#brief` (Step 1) | 用途・印象などの入力フォーム |
| AI生成中 | `#proposals` (生成アニメーション) | ローディング中表示 |
| 提案一覧 | `#proposals` (Step 2) | AIが算出した3案カード表示 |
| 編集画面 | `#editor` (Step 3) | 色・柄密度・衿・ロゴ位置の調整 |
| 試着画面 | `#model-check` (Step 5) | Three.jsを用いた3D試着・サイズ推奨 |
| 見積 | `#order` (Step 6 / 仕様書) | 数量別の自動単価見積表示 |
| 保存共有 | `#design-studio` (Fabric.js 保存) | Base64URLパラメータ共有・localStorage保存 |
| 問い合わせ | `#order` (Step 6) | 顧客情報の入力とお問い合わせ保存 |

---

## 5. AIヒアリング仕様

### 5-1. 必須入力
- **使用シーン**: ホテル・店舗制服、仕事、イベント・式典、観光・物販、チームウェア
- **見せたい印象**: 上品、伝統的、リゾート、現代的、大胆
- **基本色**: 海風ブルー、月桃ホワイト、琉球レッド、夜海ブラック、若葉グリーン
- **予定枚数**: 10枚〜1000枚

### 5-2. 任意入力
- **主役モチーフ**: AIに任せる、ハイビスカス、シーサー、波模様、ヤシ葉、紅型風

---

## 6. AI提案機能

AIは常に3つの方向性の提案を生成する。
- **案A (uniform)**: 安全・法人向け (フォーマル度重視)
- **案B (resort)**: トレンド・現代向け (柄サイズ大・開放的)
- **案C (heritage)**: 沖縄文化・伝統重視 (紅型・シーサー等の伝統柄)

---

## 7. 素材DB設計 (データ型と制約)

### 7-1. Pattern DB (`patterns` テーブル)
柄の定義情報。
- `id` (VARCHAR(50), PRIMARY KEY): 識別用ID
- `name` (VARCHAR(100), NOT NULL): 柄名
- `motif_tags` (TEXT[], NOT NULL): モチーフタグの配列
- `formal_score` (INT, CHECK(formal_score BETWEEN 1 AND 5)): 上品さ・フォーマル度のスコア
- `okinawa_score` (INT, CHECK(okinawa_score BETWEEN 1 AND 5)): 沖縄らしさのスコア
- `bold_score` (INT, CHECK(bold_score BETWEEN 1 AND 5)): 大胆さ・華やかさのスコア

### 7-2. Motif DB (`motifs` テーブル)
沖縄モチーフのメタ情報。
- `id` (VARCHAR(50), PRIMARY KEY)
- `name` (VARCHAR(100), UNIQUE)
- `category` (VARCHAR(50))

### 7-3. Palette DB (`palettes` テーブル)
配色管理。
- `id` (VARCHAR(50), PRIMARY KEY)
- `base_color` (CHAR(7), NOT NULL): ベース色 (HEX値)
- `accent_color` (CHAR(7), NOT NULL): アクセント色 (HEX値)
- `sub_color` (CHAR(7), NOT NULL): サブ色 (HEX値)
- `dark_color` (CHAR(7), NOT NULL): 引き締め色 (HEX値)

### 7-4. Garment DB (`garments` テーブル)
- `id` (VARCHAR(50), PRIMARY KEY)
- `name` (VARCHAR(100), NOT NULL)

---

## 8. 編集・配置機能

- **2Dエディタ (Fabric.js)**: ロゴ画像のアップロード (PNG/JPEG/WebP) および文字入力。拡大縮小、回転、削除、PNG書き出しをサポート。

---

## 9. 3D試着・サイズレポート

- **Three.js ビューア**:
  - 男・女の3Dマネキンモデルのレンダリング。
  - 身長、胸囲、ウエスト、肩幅を入力してアバター体型をインタラクティブに変更。
  - 胸まわりのゆとり量 (+12cm以上で推奨)、肩幅の余裕、着丈差を計算し、S/M/L/LL/3L から最適なサイズを提案する。

---

## 10. DBテーブル一覧

| テーブル名 | 内容 | 備考 |
|---|---|---|
| `users` | システムのユーザー | UUID主キー |
| `organizations` | 法人情報 | 制服大口発注用 |
| `design_sessions` | AIヒアリングセッション | 条件・ステータス保存 |
| `patterns` | 柄テンプレート | スコア値保持 |
| `palettes` | カラーパレット | HEX値定義 |
| `logo_layouts` | ロゴの配置箇所定義 | UUID主キー |
| `concepts` | 提案コンセプト文 | A/B/C案別の方向性テキスト |
| `generated_designs` | セッション別のAI提案内容 | fabricJson等のエディタ状態も保持 |
| `estimates` | 数量別概算見積 | 単価・合計 |
| `inquiries` | お問い合わせ内容 | 顧客名、メール、相談文 |

---

## ⚡ 非機能要件

### ⚡ レスポンスタイム
- AIデザイン生成（モックモード含む）: 1.5秒以内
- 3Dモデル表示・リサイズ更新: 100ms以内の描画完了（60fps維持）
- ページ遷移・タブ切り替え: 瞬時（遅延なし）

### 👥 同時接続数
- MVPプロトタイプ: ローカル実行のため制限なし
- 本番想定（サーバー構成時）: 同時アクティブユーザー 500人、瞬間最大 50人/秒

### 💾 データ保持期間
- `localStorage` 保存データ: ユーザーがブラウザキャッシュをクリアするまで半永久的に保持（セッション上限20件、問い合わせ履歴50件）
- 本番データベース: 問い合わせデータは最低7年間（商談管理用として法律に準拠）保持

---

## 📖 用語集

| 用語 | 定義 |
|---|---|
| **かりゆしウェア** | 沖縄県で製造され、沖縄らしさを表現した夏用シャツ。制服やフォーマルウェアとして広く認知されている。 |
| **開襟 (Aloha Collar)** | 襟元が開いたオープンカラースタイル。リゾート感やラフな雰囲気に適している。 |
| **ボタンダウン (BD)** | 襟の先端をボタンで身頃に留めるスタイル。制服やビジネスシーンなど、フォーマル度を高めるのに用いられる。 |
| **Fabric.js** | 2Dのキャンバス操作ライブラリ。ロゴ画像のアップロードや文字配置、ドラッグ操作などを実現する。 |
| **Three.js** | WebGLを用いてブラウザ上に3Dグラフィックスを描画するライブラリ。本システムではマネキンの描画に使用。 |
| **SRI (Subresource Integrity)** | CDNなど外部から読み込むスクリプトの改ざんを防ぐため、ハッシュ値を用いて検証するセキュリティ機能。 |
| **RLS (Row Level Security)** | データベースの行レベルセキュリティ。ユーザーが自分以外のデータを閲覧・変更できないように制限する。 |
