# コントリビューションガイドライン (Contribution Guide)

オリジナルかりゆしウェアデザイン開発システムへの開発参加、バグ修正、機能追加を行っていただくためのガイドラインです。

---

## 🛠 開発環境のセットアップ

1. リポジトリをクローンします。
2. Node.js (>= 18.0.0) がインストールされていることを確認します。
3. ルートディレクトリで `npm install` を実行し、開発に必要なライブラリをセットアップします。
4. `npm run dev` で開発用サーバーを起動して動作確認を行います。

---

## 🌿 ブランチ戦略

本プロジェクトでは GitHub Flow またはシンプルな Git Flow を採用します。

- `main`: リリース可能な本番用コードが常に配置されるブランチ。
- `develop`: 開発中の最新コードが集約されるブランチ。
- 機能開発ブランチ: `develop` から分岐し、以下の命名規則に従って作成します。
  - 新機能追加: `feature/<feature-name>` (例: `feature/tryon-3d-model`)
  - バグ修正: `bugfix/<issue-id-or-name>` (例: `bugfix/url-json-injection`)
  - ドキュメント整備: `docs/<doc-topic>` (例: `docs/update-api-contract`)

---

## ✍ コミットメッセージ規約

コミットメッセージは、何をなぜ変更したかを他者が理解しやすいように記載します。可能な限り Angular コミット規約に準拠してください。

### 形式
```
<type>(<scope>): <subject>

<body>
```

### 主要な Type
- `feat`: 新しい機能の追加
- `fix`: バグの修正
- `docs`: ドキュメントのみの変更
- `style`: コードの動作に影響しない見た目の修正（インデント、セミコロン等）
- `refactor`: バグ修正や機能追加を行わないコードの整理・最適化
- `test`: テストコードの追加・修正
- `chore`: ビルドツールや設定ファイルなどの変更

例:
```
feat(editor): add file magic byte check for logo uploading

ロゴアップロード時のセキュリティ向上のため、MIMEタイプだけでなくファイル先頭のマジックバイトを検証する処理を追加しました。
```

---

## 📥 プルリクエスト (PR) の手順

1. 機能開発用ブランチで作業を行い、コミット・プッシュします。
2. `develop` ブランチに向けてプルリクエストを作成します。
3. PR作成テンプレートに、変更内容、対応した問題、検証結果（テスト結果）を簡潔に記載します。
4. 自動化テスト（CI/CD）がパスすることを確認します。
5. 最低1名以上の開発者のレビュー・承認を得た後、マージを行います。

---

## 🎨 コーディングスタイル

- **JavaScript/ESM**: 厳格モード（`"use strict"`）を使用。
- **フォーマット**: ESLint の基準に準拠。インデントはスペース2マス。
- **命名規則**:
  - 変数・関数名: キャメルケース (`camelCase`)。1文字・2文字の極端に短い変数名（例: `V`, `g`）は避け、可読性の高い名前にしてください。
  - クラス名: パスカルケース (`PascalCase`)。
  - 定数: 大文字スネークケース (`UPPER_SNEAK_CASE`)。

---

## 🧪 テストの実行方法

変更を加えた後は、必ず既存のテストが壊れていないか確認してください。

- **ユニットテストの実行**: `npm test`
- **カバレッジの測定**: `npm run test:coverage`
- **Linter の実行**: `npm run lint`
- **スモークテストの実行**: `npm run smoke`
